### Content Summary

* **Project Overview:** A direct, clean introduction highlighting MongoDB CRUD tasks inside the terminal database wrapper.
* **Local Configuration:** Clean setup steps to verify installation via the command prompt before opening `mongosh`.
* **Structured Snippets:** Pre-formatted query boxes matching each milestone point of your track requirements.
* **Logic Highlight:** A callout note clarifying why the `$regex` query accurately returns an empty set due to data conditions. This query successfully executes but returns an empty array because no contact in the initial dataset simultaneously satisfies both conditions (Age > 18 AND Name containing 'ah')
